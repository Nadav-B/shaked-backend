version: '3.3'

services:
  database:
    container_name: database
    image: mysql:8.0.30
    command: --authentication_policy=mysql_native_password
    environment:
      MYSQL_ROOT_PASSWORD: password
      MYSQL_DATABASE: shaked
    ports: 
      - "3306:3306"
  
  